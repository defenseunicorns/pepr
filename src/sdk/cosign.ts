// SPDX-License-Identifier: Apache-2.0
// SPDX-FileCopyrightText: 2023-Present The Pepr Authors

/**
 * Returns all containers in a pod
 * @param regRef the combined registry & image reference
 * @param containerType the type of container to get
 * @returns the list of containers in the pod
 */
export function verifyImage(regRef: string, pubkeyDefs: string) {
  console.log("regRef:", regRef, "pubkeyDefs:", pubkeyDefs);
  return true;
}
