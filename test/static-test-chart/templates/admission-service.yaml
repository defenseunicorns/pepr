apiVersion: v1
kind: Service
metadata:
  name: pepr-static-test
  namespace: pepr-system
  labels:
    pepr.dev/controller: admission
spec:
  selector:
    app: pepr-static-test
    pepr.dev/controller: admission
  ports:
    - port: 443
      targetPort: 3000
